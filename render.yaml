services:
  - type: web
    name: agentic-erp-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python mock_api.py
    plan: free
    envVars:
      - key: PYTHONPATH
        value: "."
      - key: SECRET_KEY
        generateValue: true
